import React from 'react';

const Default = () => {

    return (
        <div>
        <h2>Welcome in the C.R.U.D app!</h2>
        <p>Try the basic functionnalities of user account management. You can start by creating an account (click on Sign Up).</p>
      </div>
    );
};

export default Default;